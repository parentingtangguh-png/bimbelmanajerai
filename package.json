{
  "name": "bimbel-manager",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node scripts/check-imports.mjs",
    "test": "node scripts/check-imports.mjs && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.57.4"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.3.10",
    "@playwright/test": "^1.63.0",
    "vite": "7.1.5"
  }
}
